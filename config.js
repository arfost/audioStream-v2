module.exports = {
    // Deploy task
    deploy: {
        //empty for now
    },
    firebase: {
        env: {
            default: {
                url: "https://audiostream-89853.firebaseio.com",
                config: {}
            }
        },
        global: {
            config: {
                //empty too
            }
        }
    }
}
